<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="color-scheme" content="light dark" />
  <title>VM - Se connecter</title>
  <meta name="description" content="Il faut se connecter, c'est tout" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2.1.1/css/pico.min.css" />
  <style>
    header { position: sticky; top: 0; z-index: 10; background-color: rgba(240,240,240,.7); backdrop-filter: blur(4px); border-bottom: 1px solid rgba(0,0,0,.05); }
    header nav { max-width: 1000px; margin: 0 auto; padding: .5rem 1rem; }
    body { min-height: 100dvh; display: flex; flex-direction: column; }
    main.page { flex: 1; display: flex; align-items: center; justify-content: center; padding: 2rem 1rem; }
    .content { display: flex; flex-direction: column; gap: 1.25rem; width: min(680px, 100%); }
    .block { padding: 1rem 1.25rem; border-radius: .5rem; background: var(--pico-card-background-color); box-shadow: var(--pico-card-box-shadow); }
    .block h2, .block h3 { margin: 0 0 .5rem 0; }
    form .actions { display: flex; gap: .75rem; flex-wrap: wrap; }
    @media (min-width: 960px) { .content { gap: 1.5rem; } .block { padding: 1.25rem 1.5rem; } }
  </style>
</head>
<body>
  <header>
    <nav>
      <ul>
        <li>
          <a href="/" style="display:inline-block;">
            <img src="https://www.nomadindesign.com/wp-content/uploads/2014/04/logo-moche.png" alt="Logo VM" style="height:80px; vertical-align:middle;">
          </a>
        </li>
        <details class="dropdown">
          <summary role="button" class="secondary">Accueil</summary>
          <ul class="dropdown-menu">
            <li><a href="/liste/v/m">Liste des VM</a></li>
          </ul>
        </details>
      </ul>
    </nav>
  </header>

  <main class="page">
    <section class="content">

      <article class="block">
        <h2>Connexion</h2>
        <p>Identifiez-vous pour accéder à votre espace.</p>
      </article>

      <article class="block">
        {% if error %}
          <div class="alert alert-danger" role="alert">
            {{ error.messageKey|trans(error.messageData, 'security') }}
          </div>
        {% endif %}

        <form method="post" action="{{ path('app_connection') }}">
          <label for="inputEmail">Email</label>
          <input
            type="email"
            id="inputEmail"
            name="_username"
            value="{{ last_username|default('') }}"
            autocomplete="email"
            required
            autofocus
          >

          <label for="inputPassword">Mot de passe</label>
          <input
            type="password"
            id="inputPassword"
            name="_password"
            autocomplete="current-password"
            required
          >

          <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

          <div class="actions">
            <button type="submit">Se connecter</button>
            <a role="button" class="secondary" href="{{ path('app_register') }}">Créer un compte</a>
          </div>
        </form>
      </article>

      <article class="block">
        <h3>Autres options</h3>
        <div class="cta">
          <a role="button" class="contrast" href="{{ path('connect_github_start') }}">Se connecter via GitHub</a>
        </div>
      </article>

    </section>
  </main>
</body>
</html>
